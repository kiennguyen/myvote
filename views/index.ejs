<% if (!user) { %>
	<p>Welcome to BNP  Hackday! Please <a href="/login">log in BIP</a>.</p>
<% } else { %>
	<p>Hello, <%= user.email %>. View your <a href="/profile">profile</a>.</p>
	<% if (user.email === 'kien.nguyen@niteco.se') { %>
		You can create a vote <a href="/vote/create_vote_form">here</a>
	<% } %>
<% } %>
